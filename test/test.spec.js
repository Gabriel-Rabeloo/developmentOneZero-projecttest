const { expect } = require('chai');

describe('Sample Test', () => {
  it('should test the null === null', () => {
    const result = null;
    expect(result).to.be.null;
  });
  it('should test the true === true', () => {
    const result = null;
    expect(result).to.be.true;
  });
});
